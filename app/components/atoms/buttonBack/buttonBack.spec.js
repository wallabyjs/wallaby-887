

describe('ButtonBackToTopController', () => {
  beforeEach(angular.mock.module('some mod'));

  beforeEach(inject(($controller) => {
  }));

  describe('scrollToTop()', () => {
    it('should call the scroll to top function of the service', () => {
    });
  });
});